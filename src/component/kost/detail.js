import React, { Component } from 'react';

class Detail extends Component {
   render() {
      return (
         <div>
            {this.props.name}
         </div>
      );
   }
}

export default Detail;